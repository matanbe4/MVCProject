@model MVCProject.ViewModel.UserViewModel
@using MVCProject.Models;
@{
    ViewBag.Title = "SearchUser";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Search User</title>
    <style type="text/css">
        body {
            font-family: Arial;
            font-size: 13pt;
        }

        .Grid {
            border: 1px solid #ccc;
            border-collapse: collapse;
        }

            .Grid th {
                background-color: #F7F7F7;
                font-weight: bold;
            }

            .Grid th, .Grid td {
                padding: 5px;
                border: 1px solid #ccc;
            }

            .Grid, .Grid table td {
                border: 0px solid #ccc;
            }

                .Grid th a, .Grid th a:visited {
                    color: #333;
                }
    </style>
</head>
<body>
    <div>
        <form action="SearchUser" method="post">
            <br />
            <table>
                <tr>
                    <td>Enter Username to search:</td>
                    <td><input id="Text" type="text" name="username" value="@Model.user.username" /></td>
                </tr>
                <tr>
                    <td><input id="Submit" type="submit" value="Search User" /></td>
                </tr>
            </table>
        </form>
    </div>
    <br />
    <b>Search Results:</b>
    <br />
    <div>
        @{
            WebGrid obj = new WebGrid(Model.users, canPage: false, canSort: false);
        }
        @obj.GetHtml(htmlAttributes: new { @id = "WebGrid", @class = "Grid" },
        columns: obj.Columns(
                 obj.Column("Username", "Username"),
                 obj.Column("Password", "Password")))
    </div>

</body>
</html>